const initialState = []

export function WalletReducer(state = initialState, action) {
    switch(action.type){
        case ADD_ASSET :
            return state
        default :
            return state
    }

}